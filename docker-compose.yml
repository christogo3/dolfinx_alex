version: "3"
services:
  rve-scripts:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./shared:/home
    working_dir: /home
    deploy:
      resources:
        limits:
          cpus: 10
          memory: 24G
